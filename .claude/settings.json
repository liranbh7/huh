{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path',''); print(f) if f.endswith('.go') else None\" | { read -r f; [ -n \"$f\" ] && command -v gofmt >/dev/null 2>&1 && gofmt -w \"$f\"; } 2>/dev/null || true",
            "timeout": 10,
            "statusMessage": "Formatting Go file..."
          }
        ]
      }
    ]
  },
  "enabledPlugins": {
    "skill-creator@claude-plugins-official": true
  }
}
